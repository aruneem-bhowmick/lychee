import React from 'react';

export const dynamic = 'force-static';

/**
 * Minimal placeholder landing page component.
 * Render a basic text for initial validation.
 *
 * @returns The landing page main element.
 */
export default function Home(): JSX.Element {
  return (
    <main>
      <h1>Lychee</h1>
    </main>
  );
}
